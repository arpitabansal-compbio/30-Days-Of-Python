# feat: implement a standard list processing loop to parse data sequences

# Define a structured text list representing genomic markers
target_sequences = ["ATCG", "GCTA", "TAGC"]

# Execute a clean for loop to iterate through the data array automatically
for sequence in target_sequences:
    print(f"Analyzing automated sequence block: {sequence}")
